/* @generated by lenso-contract-codegen; do not edit. */
import * as lensoContractRuntime from "@lenso/contract-runtime";

export const CAPABILITY_ID = "lenso.auth.account-admin@1";
export const DESCRIPTOR_VERSION = "1.0.0";
export const DESCRIPTOR_DIGEST = "sha256:fa2514ed403327d8dbba88787e0cec1233c289656c6d93cb9bf225b0708af49b";
export const PORTABLE = true;
export const CROSS_LANE_TRANSFER = true;

export type Int64 = lensoContractRuntime.Int64;
export type Uint64 = lensoContractRuntime.Uint64;
export type Bytes = lensoContractRuntime.Bytes;
export type Timestamp = lensoContractRuntime.Timestamp;
export type Duration = lensoContractRuntime.Duration;
export type OptionalValue<T> = lensoContractRuntime.OptionalValue<T>;
export type InvocationContext = lensoContractRuntime.InvocationContext;
export type RuntimeFailure = lensoContractRuntime.RuntimeFailure;
export type UnknownDomainError = lensoContractRuntime.UnknownDomainError;
export type StreamEvent<Message, DomainError> = lensoContractRuntime.StreamEvent<Message, DomainError>;
export type StreamSession<Message, DomainError> = lensoContractRuntime.StreamSession<Message, DomainError>;

export interface CapabilityContractReference<Client> extends CapabilityDependencyBinding<Client> {
  readonly capability_id: string;
  readonly descriptor_version: string;
  readonly descriptor_digest: string;
  readonly generated_client: string;
  readonly __client?: Client;
}

export interface ListSessionsRequest {
  cursor: string | null;
  limit: number;
  subject: string | null;
}

export interface ListSessionsResponse {
  next_cursor: string | null;
  sessions: Array<ListSessionsResponseSessionsItem>;
}

export interface ListSessionsResponseSessionsItem {
  actor_kind: string;
  assurance: string;
  created_at: Timestamp;
  expires_at: Timestamp;
  revoked: boolean;
  session_id: string;
  subject: string;
}

export interface ListSubjectsRequest {
  cursor: string | null;
  limit: number;
}

export interface ListSubjectsResponse {
  next_cursor: string | null;
  subjects: Array<ListSubjectsResponseSubjectsItem>;
}

export interface ListSubjectsResponseSubjectsItem {
  created_at: Timestamp;
  disabled_reason: string | null;
  disabled_until: Timestamp | null;
  status: "active" | "disabled";
  subject: string;
}

export interface SetSubjectStatusRequest {
  disabled_until: Timestamp | null;
  reason: string | null;
  status: "active" | "disabled";
  subject: string;
}

export interface SetSubjectStatusResponse {
  changed: boolean;
}

export type ListSessionsError = "forbidden" | "invalid_page" | "invalid_subject" | UnknownDomainError;
export type ListSessionsInvocationError = { readonly kind: "domain"; readonly error: ListSessionsError } | { readonly kind: "runtime"; readonly error: RuntimeFailure };
export type ListSessionsResult = { readonly ok: true; readonly value: ListSessionsResponse } | { readonly ok: false; readonly error: ListSessionsInvocationError };
export type ListSubjectsError = "forbidden" | "invalid_page" | UnknownDomainError;
export type ListSubjectsInvocationError = { readonly kind: "domain"; readonly error: ListSubjectsError } | { readonly kind: "runtime"; readonly error: RuntimeFailure };
export type ListSubjectsResult = { readonly ok: true; readonly value: ListSubjectsResponse } | { readonly ok: false; readonly error: ListSubjectsInvocationError };
export type SetSubjectStatusError = "forbidden" | "invalid_status" | "invalid_subject" | "not_found" | UnknownDomainError;
export type SetSubjectStatusInvocationError = { readonly kind: "domain"; readonly error: SetSubjectStatusError } | { readonly kind: "runtime"; readonly error: RuntimeFailure };
export type SetSubjectStatusResult = { readonly ok: true; readonly value: SetSubjectStatusResponse } | { readonly ok: false; readonly error: SetSubjectStatusInvocationError };
export function encodeListSessionsRequest(value: ListSessionsRequest): string { return lensoContractRuntime.encodePortableJson(value, "request"); }
export function decodeListSessionsRequest(wire: string): ListSessionsRequest { return lensoContractRuntime.decodePortableJson<ListSessionsRequest>(wire); }
export function encodeListSessionsResponse(value: ListSessionsResponse): string { return lensoContractRuntime.encodePortableJson(value, "response"); }
export function decodeListSessionsResponse(wire: string): ListSessionsResponse { return lensoContractRuntime.decodePortableJson<ListSessionsResponse>(wire); }
export function encodeListSessionsError(value: ListSessionsError): string { return lensoContractRuntime.encodePortableJson(value, "Domain Error"); }
export function decodeListSessionsError(wire: string): ListSessionsError { return lensoContractRuntime.decodeDomainError<ListSessionsError>(wire, ["forbidden", "invalid_page", "invalid_subject"]); }

export function encodeListSubjectsRequest(value: ListSubjectsRequest): string { return lensoContractRuntime.encodePortableJson(value, "request"); }
export function decodeListSubjectsRequest(wire: string): ListSubjectsRequest { return lensoContractRuntime.decodePortableJson<ListSubjectsRequest>(wire); }
export function encodeListSubjectsResponse(value: ListSubjectsResponse): string { return lensoContractRuntime.encodePortableJson(value, "response"); }
export function decodeListSubjectsResponse(wire: string): ListSubjectsResponse { return lensoContractRuntime.decodePortableJson<ListSubjectsResponse>(wire); }
export function encodeListSubjectsError(value: ListSubjectsError): string { return lensoContractRuntime.encodePortableJson(value, "Domain Error"); }
export function decodeListSubjectsError(wire: string): ListSubjectsError { return lensoContractRuntime.decodeDomainError<ListSubjectsError>(wire, ["forbidden", "invalid_page"]); }

export function encodeSetSubjectStatusRequest(value: SetSubjectStatusRequest): string { return lensoContractRuntime.encodePortableJson(value, "request"); }
export function decodeSetSubjectStatusRequest(wire: string): SetSubjectStatusRequest { return lensoContractRuntime.decodePortableJson<SetSubjectStatusRequest>(wire); }
export function encodeSetSubjectStatusResponse(value: SetSubjectStatusResponse): string { return lensoContractRuntime.encodePortableJson(value, "response"); }
export function decodeSetSubjectStatusResponse(wire: string): SetSubjectStatusResponse { return lensoContractRuntime.decodePortableJson<SetSubjectStatusResponse>(wire); }
export function encodeSetSubjectStatusError(value: SetSubjectStatusError): string { return lensoContractRuntime.encodePortableJson(value, "Domain Error"); }
export function decodeSetSubjectStatusError(wire: string): SetSubjectStatusError { return lensoContractRuntime.decodeDomainError<SetSubjectStatusError>(wire, ["forbidden", "invalid_status", "invalid_subject", "not_found"]); }


export interface AccountAdminClient {
  list_sessions(request: ListSessionsRequest, context?: InvocationContext): Promise<ListSessionsResult>;
  list_subjects(request: ListSubjectsRequest, context?: InvocationContext): Promise<ListSubjectsResult>;
  set_subject_status(request: SetSubjectStatusRequest, context?: InvocationContext): Promise<SetSubjectStatusResult>;
}

export interface AccountAdminProvider {
  list_sessions(context: InvocationContext, request: ListSessionsRequest): Promise<ListSessionsResult>;
  list_subjects(context: InvocationContext, request: ListSubjectsRequest): Promise<ListSubjectsResult>;
  set_subject_status(context: InvocationContext, request: SetSubjectStatusRequest): Promise<SetSubjectStatusResult>;
}

export const ACCOUNT_ADMIN_CONTRACT: CapabilityContractReference<AccountAdminClient> = { ...bindAccountAdminDependency(), capability_id: CAPABILITY_ID, descriptor_version: DESCRIPTOR_VERSION, descriptor_digest: DESCRIPTOR_DIGEST, generated_client: "AccountAdminClient" };

export type ProviderDispatchOutcome =
  | { readonly kind: "success"; readonly value: unknown }
  | { readonly kind: "domain"; readonly value: unknown }
  | { readonly kind: "runtime"; readonly failure: RuntimeFailure };

export interface CapabilityProviderDescriptor {
  readonly capability_id: string;
  readonly descriptor_version: string;
  readonly operations: ReadonlyArray<string>;
  readonly stream_operations: ReadonlyArray<string>;
  readonly event_operations: ReadonlyArray<string>;
}

export interface CapabilityProviderBinding {
  readonly descriptor: CapabilityProviderDescriptor;
  invokeRequest(
    operation: string,
    context: InvocationContext,
    payload: unknown,
  ): Promise<ProviderDispatchOutcome>;
}

function providerErrorMessage(error: unknown): string {
  return error instanceof Error ? error.message : String(error);
}

export function bindAccountAdminProvider(
  provider: AccountAdminProvider,
): CapabilityProviderBinding {
  return {
    descriptor: {
      capability_id: CAPABILITY_ID,
      descriptor_version: DESCRIPTOR_VERSION,
      operations: ["list_sessions", "list_subjects", "set_subject_status"],
      stream_operations: [],
      event_operations: [],
    },
    async invokeRequest(operation, context, payload) {
      switch (operation) {
      case "list_sessions": {
        let request: ListSessionsRequest;
        try {
          request = decodeListSessionsRequest(lensoContractRuntime.encodePortableJson(payload, "request"));
        } catch (error) {
          return { kind: "runtime", failure: { kind: "protocol_violation", detail: providerErrorMessage(error) } };
        }
        try {
          const result = await provider.list_sessions(context, request);
          if (result.ok) {
            return { kind: "success", value: JSON.parse(encodeListSessionsResponse(result.value)) as unknown };
          }
          if (result.error.kind === "domain") {
            return { kind: "domain", value: JSON.parse(encodeListSessionsError(result.error.error)) as unknown };
          }
          return { kind: "runtime", failure: result.error.error };
        } catch (error) {
          return { kind: "runtime", failure: { kind: "plugin_failure", detail: providerErrorMessage(error) } };
        }
      }
      case "list_subjects": {
        let request: ListSubjectsRequest;
        try {
          request = decodeListSubjectsRequest(lensoContractRuntime.encodePortableJson(payload, "request"));
        } catch (error) {
          return { kind: "runtime", failure: { kind: "protocol_violation", detail: providerErrorMessage(error) } };
        }
        try {
          const result = await provider.list_subjects(context, request);
          if (result.ok) {
            return { kind: "success", value: JSON.parse(encodeListSubjectsResponse(result.value)) as unknown };
          }
          if (result.error.kind === "domain") {
            return { kind: "domain", value: JSON.parse(encodeListSubjectsError(result.error.error)) as unknown };
          }
          return { kind: "runtime", failure: result.error.error };
        } catch (error) {
          return { kind: "runtime", failure: { kind: "plugin_failure", detail: providerErrorMessage(error) } };
        }
      }
      case "set_subject_status": {
        let request: SetSubjectStatusRequest;
        try {
          request = decodeSetSubjectStatusRequest(lensoContractRuntime.encodePortableJson(payload, "request"));
        } catch (error) {
          return { kind: "runtime", failure: { kind: "protocol_violation", detail: providerErrorMessage(error) } };
        }
        try {
          const result = await provider.set_subject_status(context, request);
          if (result.ok) {
            return { kind: "success", value: JSON.parse(encodeSetSubjectStatusResponse(result.value)) as unknown };
          }
          if (result.error.kind === "domain") {
            return { kind: "domain", value: JSON.parse(encodeSetSubjectStatusError(result.error.error)) as unknown };
          }
          return { kind: "runtime", failure: result.error.error };
        } catch (error) {
          return { kind: "runtime", failure: { kind: "plugin_failure", detail: providerErrorMessage(error) } };
        }
      }
        default:
          return { kind: "runtime", failure: { kind: "unknown_operation", operation } };
      }
    },
  };
}

export type Provider = AccountAdminProvider;
export const bindProvider = bindAccountAdminProvider;

export type DependencyInvoker = (
  operation: string,
  context: InvocationContext,
  payload: unknown,
) => Promise<ProviderDispatchOutcome>;

export interface CapabilityDependencyBinding<Client> {
  readonly descriptor: CapabilityProviderDescriptor;
  createClient(invoke: DependencyInvoker): Client;
}

function dependencyErrorMessage(error: unknown): string {
  return error instanceof Error ? error.message : String(error);
}

export function bindAccountAdminDependency(): CapabilityDependencyBinding<AccountAdminClient> {
  return {
    descriptor: {
      capability_id: CAPABILITY_ID,
      descriptor_version: DESCRIPTOR_VERSION,
      operations: ["list_sessions", "list_subjects", "set_subject_status"],
      stream_operations: [],
      event_operations: [],
    },
    createClient(invoke) {
      return {
      async list_sessions(request, context) {
        let payload: unknown;
        try {
          payload = JSON.parse(encodeListSessionsRequest(request)) as unknown;
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "protocol_violation", detail: dependencyErrorMessage(error) } } };
        }
        const call = context ?? { requestId: "0" as Uint64, cancelled: false };
        try {
          const outcome = await invoke("list_sessions", call, payload);
          if (outcome.kind === "success") {
            return { ok: true, value: decodeListSessionsResponse(JSON.stringify(outcome.value)) };
          }
          if (outcome.kind === "domain") {
            return { ok: false, error: { kind: "domain", error: decodeListSessionsError(JSON.stringify(outcome.value)) } };
          }
          return { ok: false, error: { kind: "runtime", error: outcome.failure } };
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "plugin_failure", detail: dependencyErrorMessage(error) } } };
        }
      },
      async list_subjects(request, context) {
        let payload: unknown;
        try {
          payload = JSON.parse(encodeListSubjectsRequest(request)) as unknown;
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "protocol_violation", detail: dependencyErrorMessage(error) } } };
        }
        const call = context ?? { requestId: "0" as Uint64, cancelled: false };
        try {
          const outcome = await invoke("list_subjects", call, payload);
          if (outcome.kind === "success") {
            return { ok: true, value: decodeListSubjectsResponse(JSON.stringify(outcome.value)) };
          }
          if (outcome.kind === "domain") {
            return { ok: false, error: { kind: "domain", error: decodeListSubjectsError(JSON.stringify(outcome.value)) } };
          }
          return { ok: false, error: { kind: "runtime", error: outcome.failure } };
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "plugin_failure", detail: dependencyErrorMessage(error) } } };
        }
      },
      async set_subject_status(request, context) {
        let payload: unknown;
        try {
          payload = JSON.parse(encodeSetSubjectStatusRequest(request)) as unknown;
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "protocol_violation", detail: dependencyErrorMessage(error) } } };
        }
        const call = context ?? { requestId: "0" as Uint64, cancelled: false };
        try {
          const outcome = await invoke("set_subject_status", call, payload);
          if (outcome.kind === "success") {
            return { ok: true, value: decodeSetSubjectStatusResponse(JSON.stringify(outcome.value)) };
          }
          if (outcome.kind === "domain") {
            return { ok: false, error: { kind: "domain", error: decodeSetSubjectStatusError(JSON.stringify(outcome.value)) } };
          }
          return { ok: false, error: { kind: "runtime", error: outcome.failure } };
        } catch (error) {
          return { ok: false, error: { kind: "runtime", error: { kind: "plugin_failure", detail: dependencyErrorMessage(error) } } };
        }
      },
      };
    },
  };
}

export const bindDependency = bindAccountAdminDependency;

export const portableValueProfile = lensoContractRuntime.portableValueProfile;
